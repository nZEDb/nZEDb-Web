# Define the line ending behavior of the different file extensions
# Set default behavior, in case users don't have core.autocrlf set.
* text		text=auto eol=lf trim_trailing_whitespace=true

# Declare files that will always have CRLF line endings on checkout.
*.bat		eol=crlf

# Declare files that will always have LF line endings on checkout.
*.pem		eol=lf

# Declare text files that have specific needs over the defaults.
*.md		text conflict-marker-size=100 trim_trailing_whitespace=false
*.txt		text conflict-marker-size=100
*.vcproj	text eol=crlf
*
# Denote all files that are truly binary and should not be modified.
*.7z		binary
*.bz2		binary
*.eot		binary
*.gif		binary
*.gz		binary
*.ico		binary
*.jpeg		binary
*.jpg		binary
*.mo		binary
*.mp4		binary
*.ogv		binary
*.otf		binary
*.pdf		binary
*.phar		binary
*.png		binary
*.ttf		binary
*.webm		binary
*.webp		binary
*.xls		binary
*.xlsx		binary
*.woff		binary
*.woff2		binary
*.zip		binary

# Declare specific file's properties
Changelog		text eol=lf trim_trailing_whitespace=true
ReleaseNotes	text eol=lf trim_trailing_whitespace=true
.gitignore		text eol=lf trim_trailing_whitespace=true
.gitattributes	text eol=lf trim_trailing_whitespace=true
.gitmodules     text eol=lf trim_trailing_whitespace=true
composer.lock	text eol=lf trim_trailing_whitespace=true
